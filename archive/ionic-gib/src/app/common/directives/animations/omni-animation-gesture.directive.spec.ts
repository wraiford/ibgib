import { OmniAnimationGestureDirective } from './omni-animation-gesture.directive';

describe('ClickAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new OmniAnimationGestureDirective(null, null, null, null);
    expect(directive).toBeTruthy();
  });
});
