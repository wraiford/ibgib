import { ClickAnimationDirective } from './click-animation.directive';

describe('ClickAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
