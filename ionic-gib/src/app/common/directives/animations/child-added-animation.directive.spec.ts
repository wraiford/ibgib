import { ChildAddedAnimationDirective } from './child-added-animation.directive';

describe('ChildAddedAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildAddedAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
