import { FadeOutAnimationDirective } from './fade-out-animation.directive';

describe('FadeOutAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOutAnimationDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
