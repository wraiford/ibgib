import { ClickHoldAnimationDirective } from './click-hold-animation.directive';

describe('ClickAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickHoldAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
